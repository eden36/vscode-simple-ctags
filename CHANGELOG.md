# 更新日志

## 0.1.0

- 首次实现基于经典、已排序 tags 的语言无关定义跳转。
- 支持多根工作区、Remote URI、限定上下文评分、受限缓存与诊断命令。
- 新增“生成或更新 Tags”命令：文件名取自 `tagFileNames` 首项，默认排除常见构建与依赖目录，带输出上限、超时与取消。
- 改为按命令激活，不再声明 `onStartupFinished`；右键菜单项限定在受信任工作区的文本编辑器中显示。
- 同一目标文件的候选合并为一次顺序扫描，取代逐候选的重复全文件扫描。
- tags 定位缓存的命中结果加入 TTL，在更靠近当前文件的目录新建 tags 后可自然生效。
